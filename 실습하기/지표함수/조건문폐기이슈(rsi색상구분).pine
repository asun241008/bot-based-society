
indicator("RSI 신호 지표")

hline(70) //과매수 수치
hline(30) //과매도 수치

rsi = ta.rsi(close, 14)
rsiColor = rsi > 70 ? color.green : 
           rsi > 50 ? color.yellow : 
           rsi > 30 ? color.purple : color.red

plot(rsi, color = rsiColor)
