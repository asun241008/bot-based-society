//@version=6
strategy("메세지 테스트", overlay=true, fill_orders_on_standard_ohlc = true)

// 주문 명령 안에 메세지 붙여 넣고, 설정창에서 치환 코드도 복붙하다.
strategy.entry("롱", strategy.long, alert_message = str.format("{0} {1} 진입!", syminfo.ticker, syminfo.prefix))
strategy.close("롱", alert_message = str.format("{0} {1} 청산!", syminfo.ticker, syminfo.prefix))
